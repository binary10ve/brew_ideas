// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
function IdeasApp(){


	this.boot();
}

IdeasApp.prototype.boot = function() {
	
	
};

IdeasApp.prototype.reinitialize = function(first_argument) {
	
};

IdeasApp.prototype.destroy = function(first_argument) {
	
};